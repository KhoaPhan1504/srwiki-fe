import { DashboardLayout } from '~root/components/layout';
import { RegexTesterScreen as RegexTester } from '~root/screens/regex-tester';

const RegexTesterPage = () => (
  <DashboardLayout>
    <RegexTester />
  </DashboardLayout>
);

export default RegexTesterPage;
