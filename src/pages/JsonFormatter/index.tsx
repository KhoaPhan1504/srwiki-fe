import { DashboardLayout } from '~root/components/layout';
import { JsonFormatterPage as JsonFormatter } from '~root/screens/json-formatter';

const JsonFormatterPage = () => (
  <DashboardLayout>
    <JsonFormatter />
  </DashboardLayout>
);

export default JsonFormatterPage;
