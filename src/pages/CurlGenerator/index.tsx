import { DashboardLayout } from '~root/components/layout';
import { CurlGeneratorScreen } from '~root/screens/curl-generator';

const CurlGeneratorPage = () => (
  <DashboardLayout>
    <CurlGeneratorScreen />
  </DashboardLayout>
);

export default CurlGeneratorPage;
