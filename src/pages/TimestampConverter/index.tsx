import { DashboardLayout } from '~root/components/layout';
import { TimestampConverterScreen } from '~root/screens/timestamp-converter';

const TimestampConverterPage = () => (
  <DashboardLayout>
    <TimestampConverterScreen />
  </DashboardLayout>
);

export default TimestampConverterPage;
