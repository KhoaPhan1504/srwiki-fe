import { DashboardLayout } from '~root/components/layout';
import { AdminMembersScreen } from '~root/screens/admin-members';

const AdminMembersPage = () => (
  <DashboardLayout>
    <AdminMembersScreen />
  </DashboardLayout>
);

export default AdminMembersPage;
