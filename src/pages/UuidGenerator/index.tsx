import { DashboardLayout } from '~root/components/layout';
import { UuidGeneratorPage as UuidGenerator } from '~root/screens/uuid-generator';

const UuidGeneratorPage = () => (
  <DashboardLayout>
    <UuidGenerator />
  </DashboardLayout>
);

export default UuidGeneratorPage;
